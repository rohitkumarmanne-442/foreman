# Foreman is local-first and needs NO secrets to run — everything stays on
# 127.0.0.1. These optional vars only apply if you wire up integrations.

# Optional: create Jira tickets from flagged sessions (Settings → Jira).
JIRA_API_TOKEN=

# Optional: override the inbox port (default 4517).
FOREMAN_PORT=4517
